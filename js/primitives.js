// PRIMITIVES

// ...basically the simplest form of a data type = THE data types


const string = new String("hello");  //this actually should not really be called / defined somewhere - js takes care
const stringOther = 'hello';

console.log(string === stringOther);
console.log(string == stringOther);

// this actually works - wrapping in js object
console.log('some string'.toUpperCase());

